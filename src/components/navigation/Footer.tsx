'use client';

import { Container } from '@mui/material';

const Footer = () => {
	return (
		<footer className='fixed inset-x-0 bottom-0 top-auto bg-primary-900'>
			<Container maxWidth='lg' className='h-10'></Container>
		</footer>
	);
};

export default Footer;
